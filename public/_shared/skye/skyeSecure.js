// Generated by scripts/sync-skye-runtime.js from src/lib/skye/skyeSecure.ts
export const SKYE_BINARY_MARKER = "SKYESEC1";
export const SKYE_SECURE_FORMAT = "skye-secure-v1";
export const SKYE_SECURE_ALG = "AES-256-GCM";
export const SKYE_SECURE_KDF = "PBKDF2-SHA256";
export const SKYE_DEFAULT_ITERATIONS = 120000;
function isRecord(value) {
    return typeof value === "object" && value !== null;
}
function toArrayBuffer(bytes) {
    return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
}
export function bytesToBase64(bytes) {
    let binary = "";
    for (let i = 0; i < bytes.length; i += 1)
        binary += String.fromCharCode(bytes[i]);
    return btoa(binary);
}
export function base64ToBytes(value) {
    const binary = atob(value);
    const bytes = new Uint8Array(binary.length);
    for (let i = 0; i < binary.length; i += 1)
        bytes[i] = binary.charCodeAt(i);
    return bytes;
}
export function isSkyeEncryptedBlock(value) {
    return Boolean(isRecord(value) &&
        typeof value.cipher === "string" &&
        value.cipher.length > 0 &&
        typeof value.iv === "string" &&
        value.iv.length > 0 &&
        typeof value.salt === "string" &&
        value.salt.length > 0);
}
export function validateSkyeEnvelope(value, options = {}) {
    const expectedIterations = options.expectedIterations ?? SKYE_DEFAULT_ITERATIONS;
    if (!isRecord(value))
        return false;
    if (value.format !== SKYE_SECURE_FORMAT)
        return false;
    if (value.encrypted !== true)
        return false;
    if (value.alg !== SKYE_SECURE_ALG)
        return false;
    if (value.kdf !== SKYE_SECURE_KDF)
        return false;
    if (Number(value.iterations) !== expectedIterations)
        return false;
    if (typeof value.exportedAt !== "string" || value.exportedAt.length === 0)
        return false;
    if (!isRecord(value.payload) || !isSkyeEncryptedBlock(value.payload.primary))
        return false;
    if (value.payload.failsafe != null && !isSkyeEncryptedBlock(value.payload.failsafe))
        return false;
    return true;
}
export function validateSkyePlainPayload(value) {
    if (!isRecord(value) || !isRecord(value.meta) || !isRecord(value.state))
        return false;
    if (typeof value.meta.app_id !== "string" || value.meta.app_id.length === 0)
        return false;
    if (!Number.isFinite(Number(value.meta.schema_version)))
        return false;
    if (value.assets == null)
        return true;
    if (!Array.isArray(value.assets))
        return false;
    return value.assets.every((asset) => isRecord(asset) &&
        typeof asset.id === "string" &&
        typeof asset.name === "string" &&
        typeof asset.mime === "string" &&
        typeof asset.data_base64 === "string");
}
export async function deriveSkyeKey(passphrase, salt, iterations = SKYE_DEFAULT_ITERATIONS) {
    const baseKey = await crypto.subtle.importKey("raw", new TextEncoder().encode(passphrase), "PBKDF2", false, ["deriveKey"]);
    return crypto.subtle.deriveKey({ name: "PBKDF2", salt: toArrayBuffer(salt), iterations, hash: "SHA-256" }, baseKey, { name: "AES-GCM", length: 256 }, false, ["encrypt", "decrypt"]);
}
export async function encryptSkyePayload(plainText, passphrase, options = {}) {
    const iv = crypto.getRandomValues(new Uint8Array(12));
    const salt = crypto.getRandomValues(new Uint8Array(16));
    const key = await deriveSkyeKey(passphrase, salt, options.iterations ?? SKYE_DEFAULT_ITERATIONS);
    const cipher = await crypto.subtle.encrypt({ name: "AES-GCM", iv: toArrayBuffer(iv) }, key, toArrayBuffer(new TextEncoder().encode(plainText)));
    return {
        cipher: bytesToBase64(new Uint8Array(cipher)),
        iv: bytesToBase64(iv),
        salt: bytesToBase64(salt),
    };
}
export async function decryptSkyePayload(block, passphrase, options = {}) {
    const iv = base64ToBytes(block.iv);
    const salt = base64ToBytes(block.salt);
    const key = await deriveSkyeKey(passphrase, salt, options.iterations ?? SKYE_DEFAULT_ITERATIONS);
    const plain = await crypto.subtle.decrypt({ name: "AES-GCM", iv: toArrayBuffer(iv) }, key, toArrayBuffer(base64ToBytes(block.cipher)));
    return new TextDecoder().decode(plain);
}
export function buildSkyeSecureEnvelope(input) {
    return {
        format: SKYE_SECURE_FORMAT,
        encrypted: true,
        alg: SKYE_SECURE_ALG,
        kdf: SKYE_SECURE_KDF,
        iterations: input.iterations ?? SKYE_DEFAULT_ITERATIONS,
        exportedAt: input.exportedAt ?? new Date().toISOString(),
        hint: input.hint?.trim() || undefined,
        payload: {
            primary: input.primary,
            ...(input.failsafe ? { failsafe: input.failsafe } : {}),
        },
    };
}
export function serializeSkyeEnvelope(envelope) {
    const marker = new TextEncoder().encode(SKYE_BINARY_MARKER);
    const payload = new TextEncoder().encode(JSON.stringify(envelope));
    return new Blob([marker, new Uint8Array([0]), payload], { type: "application/octet-stream" });
}
export function parseSkyeEnvelope(raw, options = {}) {
    const parsed = JSON.parse(raw);
    if (!validateSkyeEnvelope(parsed, options)) {
        throw new Error("Invalid .skye secure envelope.");
    }
    return parsed;
}
export async function readSkyeEnvelopeFromBlob(blob, options = {}) {
    const bytes = new Uint8Array(await blob.arrayBuffer());
    const marker = new TextEncoder().encode(SKYE_BINARY_MARKER);
    const hasMarker = bytes.length > marker.length + 1 && marker.every((value, index) => bytes[index] === value) && bytes[marker.length] === 0;
    if (!hasMarker) {
        throw new Error("Invalid .skye file marker.");
    }
    const raw = new TextDecoder().decode(bytes.slice(marker.length + 1));
    return parseSkyeEnvelope(raw, options);
}
export function normalizeLegacySkyeEnvelope(input) {
    if (isRecord(input) && input.format === "skye-v2" && typeof input.app === "string") {
        return {
            kind: "skye-v2-json",
            legacy: input,
        };
    }
    if (typeof input === "string") {
        const legacyPrefix = `${SKYE_BINARY_MARKER}\n`;
        if (input.startsWith(legacyPrefix)) {
            const candidate = JSON.parse(input.slice(legacyPrefix.length));
            if (validateSkyeEnvelope(candidate)) {
                return {
                    kind: "text-prefixed-secure-envelope",
                    envelope: candidate,
                };
            }
        }
    }
    return null;
}
