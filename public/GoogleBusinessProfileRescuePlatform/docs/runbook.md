# Operator Runbook

- Log in with the bootstrap account on first deploy.
- Rotate credentials immediately.
- Run a bootstrap case, diagnosis, export, and monitoring check before inviting users.
- Keep `SIGNING_SECRET` stable or export signatures will drift.
