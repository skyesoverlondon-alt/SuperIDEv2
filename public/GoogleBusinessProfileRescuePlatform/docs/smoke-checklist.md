# Smoke Checklist

1. /v1/health returns 200
2. /v1/session returns demo or logged-in user
3. login works against D1
4. create case works
5. diagnosis route returns text
6. letter route returns text
7. export route returns JSON pack + signature
