# Backup and Restore

Use `wrangler d1 export` for snapshotting the D1 database and copy critical R2 evidence keys to a backup bucket. The platform is designed so D1 contains metadata and R2 contains evidence blobs and smoke artifacts.
