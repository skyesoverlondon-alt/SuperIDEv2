export const monitoringPrompt = `
You are checking a Google Business Profile recovery case.
Summarize whether the listing appears healthy, suspended, duplicate-prone, or verification-blocked.
Return a short operational note and recommended next action.
`.trim();
