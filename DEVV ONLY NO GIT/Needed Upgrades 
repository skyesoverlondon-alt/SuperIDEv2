SUPERIDEV2 UPGRADE PLAN (POST UI-DROPIN + PREVIEW STABILIZATION)
Date: 2026-03-06
Owner intent: Fix IDE preview reliability, unify workspace experience, and ship a cohesive SuperIDE upgrade path.
Constraint: Do NOT modify SkyeDocxPro, Skye-ID, SKYEMAIL-GEN internals.

==================================================
0) WHAT WAS FIXED RIGHT NOW
==================================================

[IDE PREVIEW STABILIZATION]
- [x] Updated `src/App.tsx` preview lane to use a resilient preview URL fallback path.
- [x] Added explicit iframe preview load/error handling so preview failures are visible (not silent blank panes).
- [x] Added clearer fallback behavior so the IDE preview pane always has a valid target instead of collapsing to "nothing".

What this accomplishes:
- Prevents the "preview not rendering at all" silent failure pattern.
- Gives immediate user feedback when a specific app surface fails to load.
- Keeps the workspace usable while deeper integration work is completed.


==================================================
1) STRATEGIC UPGRADE GOAL (NXT LEVEL)
==================================================

- [ ] Move SuperIDE from "many separate app pages" to one unified workspace shell.
- [ ] Use `SUPERIDE-UI-Dropin` as the standard workbench frame (editor + preview + console + file tree + status model).
- [ ] Introduce two preview lanes:
  - Quick Preview (in-memory/demo runtime for instant UX reliability)
  - Project Preview (real app/runtime surface for production-accurate behavior)
- [ ] Standardize save/run/preview/git/diagnostics behavior so every surface feels like one product.

What this accomplishes:
- A consistent user mental model.
- Faster debug and verification loop.
- Less preview fragility due to explicit runtime modes.


==================================================
2) IMPLEMENTATION PHASES
==================================================

PHASE 1: PREVIEW RELIABILITY BASELINE (NOW)
- [x] Keep current IDE live preview pane operational via fallback URL strategy.
- [x] Add visible preview error state in workspace preview frame.
- [x] Add "Preview Health" indicator chip in header/status area.
- [x] Add one-click "Open Preview in New Tab" fallback action near error state.
- [x] Add preview retry control that forces iframe refresh without losing editor state.

Outcome:
- Preview failures become diagnosable and recoverable immediately.


PHASE 2: DROPIN-POWERED WORKBENCH INTEGRATION
- [x] Mount `SUPERIDE-UI-Dropin` workbench pattern directly inside the existing SuperIDE IDE workspace (in-app, not standalone route).
- [ ] Reuse existing SuperIDE auth + workspace identity context.
- [ ] Keep design language aligned with `src/styles.css` tokens while preserving drop-in workbench ergonomics.

Outcome:
- Shared workbench shell across core SuperIDE workflows.


PHASE 3: REAL FILE MODEL INTEGRATION
- [x] Replace drop-in `defaultFiles` path with actual workspace file provider wiring (`/api/ws-get`, `/api/ws-save`) in the integrated IDE shell.
- [x] Add dirty state tracking with save-state chip and stable workspace hashing.
- [x] Add conflict-aware save semantics with reload/force-save controls.

Suggested API contracts:
- `GET /api/ide/tree`
- `GET /api/ide/file?path=...`
- `PUT /api/ide/file?path=...`
- `POST /api/ide/files/batch-save`

Outcome:
- IDE becomes source-of-truth editor, not a demo shell.


PHASE 4: RUN + PREVIEW COHESION
- [x] Add runtime selector: `Quick Preview` vs `Project Preview`.
- [x] Surface runtime mode visibly in IDE controls/status chips.
- [x] Route logs/errors into a unified preview diagnostics area.

Outcome:
- Users know exactly what preview they are looking at and why.


PHASE 5: CROSS-RUNTIME QUALITY GATES
- [ ] Enforce release gate flow before production pushes.
- [ ] Run/check sequence consistently via `/release-gate` prompt.
- [ ] Track pass/fail artifacts for procurement and enterprise handoff.

Outcome:
- Fewer regressions from drift between frontend/functions/worker behavior.


==================================================
3) HOW NEW UI CODE IS USED DIRECTLY
==================================================

`SUPERIDE-UI-Dropin` now acts as:
- Workbench UX template (layout, pane behavior, preview mode controls)
- Integration seam for real providers (file, preview, git)
- Fast path to cohesive UI upgrades without rewriting shell patterns repeatedly

Primary integration targets:
- `SUPERIDE-UI-Dropin/src/components/SuperIDEWorkbench.jsx`
- `SUPERIDE-UI-Dropin/src/lib/defaultFiles.js`
- `SUPERIDE-UI-Dropin/src/styles.css`


==================================================
4) DEFINITION OF SUCCESS
==================================================


- [ ] Preview renders reliably in all normal IDE workflows.
- [x] Preview failure states are visible and actionable (never silent).
- [x] User can switch preview runtime modes intentionally.
- [x] Workspace edit/save/preview loop is consistent across app surfaces.
- [ ] Release gate checks are run in a predictable sequence before deploy.


==================================================
5) IMMEDIATE NEXT EXECUTION (ORDERED)
==================================================

1. [x] Add preview health chip + explicit retry/open actions in IDE header.
2. [x] Introduce `previewProvider` abstraction (quick vs project runtime).
3. [x] Embed/mount `SUPERIDE-UI-Dropin` workbench pattern inside existing IDE workspace.
4. [x] Wire real file tree/file content provider into drop-in shell.
5. [ ] Run release-gate sequence and capture artifact snapshot.


==================================================
6) MASS EXECUTION TODO (FAT LIST)
==================================================

- [x] Keep AI calls gateway-only (`/api/kaixu-generate`) with no direct provider bypass.
- [x] Keep existing SuperIDE runtime and identity context (no separate app split).
- [x] Replace IDE shell with integrated drop-in rail/sidebar/editor/preview model.
- [x] Add provider-backed file load/save path (`/api/ws-get`, `/api/ws-save`).
- [x] Add stable file hashing for deterministic dirty/conflict checks.
- [x] Add conflict detection before save when server copy drifts.
- [x] Add force-save and reload-server conflict recovery actions.
- [x] Add debounced autosave toggle and persistence.
- [x] Add runtime switch controls for quick vs project preview.
- [x] Add diagnostics feed for preview/save/load events and failures.
- [x] Add preview retry telemetry and detached preview tracing.
- [ ] Add endpoint-level lazy file tree (`/api/ide/tree`) to reduce initial payloads.
- [ ] Add path-level file get/save endpoints for true on-demand hydration.
- [ ] Add release-gate run + artifacts capture and attach proof output.

